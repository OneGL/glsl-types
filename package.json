{
  "name": "glsl-types",
  "version": "0.0.0",
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown && wasm-bindgen --target nodejs --out-dir pkg ./target/wasm32-unknown-unknown/debug/glsl_types.wasm",
    "start": "npm run build && node index.js"
  },
  "dependencies": {
    "@types/node": "^20.12.13",
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  }
}
